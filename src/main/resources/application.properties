# Disable Thymeleaf caching for hot-swapping of templates
spring.thymeleaf.cache = true

# Database connection settings
spring.datasource.url=jdbc:mysql://db660282673.db.1and1.com:3306/db660282673
spring.datasource.username=dbo660282673
spring.datasource.password=121216CragsList!

# Specify the DBMS
spring.jpa.database = MYSQL

# Show or not log for each sql query
spring.jpa.show-sql = false

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect